# MetaCapybara GitHub profile package

Status: prepared locally; not applied to GitHub. Browser access was blocked because its security-policy check was unavailable. Existing organization settings and any existing .github repository have not been inspected.

- `avatar.png`: unchanged canonical character, from `ecosystem/lore/assets/capybara-idle.png`.
- `profile/assets/banner.svg`: repository-native vector composition using the original embedded character and the canonical palette from `ecosystem/design/capybara.css`.
- `profile/README.md`: proposed public organization profile for the `.github` repository. The image URL assumes its default branch is `main`; verify before applying.
- `organization.json`: proposed display name, description and website. No changes to permissions or visibility are part of this package.

Before applying, inspect the live organization and preserve any existing profile content. Merge these profile files into the public `.github` repository only after checking its current state. Set the organization avatar to `avatar.png` and use the proposed metadata. Verify the organization overview visually after saving.
